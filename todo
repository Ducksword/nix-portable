# gnome
## remove excess packages
## configure default settings for home-manager?
## add declarations for extensions

# flatpak
## add flatpak nix support has to piggy off of home-manager
install after chroot
    depends on home-manager
add support for flatpak backup and restore

```
see if home-manager can manage a script that installs
flatpak. if not just use a shell script that can be run anywhere.
```

# System related
Colors in man and less

# Borg
setup borg backup
    look into different backup options
    especially with flatpaks
    ie: different backup paths for different flatpaks

reinstall on main os
    make sure to properly setup password